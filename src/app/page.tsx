import Weather from "../components/Weather";
import Forecast from "../components/Forecast"; // forecast component layout track control handle clear out build setup

export default function Home() {
  return (
    <main className="min-h-screen bg-neutral-950">
      {/* Weather component complete layout dynamic data wrapper map system handle korbe */}
      <Weather />
    </main>
  );
}